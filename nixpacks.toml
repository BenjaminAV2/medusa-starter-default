[phases.setup]
nixPkgs = ["nodejs_20", "yarn"]

[phases.install]
cmds = ["yarn install --immutable"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "sh -c 'yarn medusa db:migrate && yarn start'"
